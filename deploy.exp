#!/usr/bin/expect -f

set filename [lindex $argv 0]
set timeout -1
spawn scp $filename team9@icdatasrv1.epfl.ch:~/
set pass "thrufbeyto"
expect {
        Password: {send "$pass\r" ; exp_continue}
        eof exit
}